import * as React from "react";

import { TwitchChannel } from "../../utils/twitch-types";

interface TwitchListChannelItemProps {
  channel: TwitchChannel;
}

export const TwitchListChannelItem: React.FC<TwitchListChannelItemProps> = () => null;
